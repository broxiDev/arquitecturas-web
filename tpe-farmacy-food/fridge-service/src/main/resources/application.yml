server:
  port: 8082

spring:
  application:
    name: fridge-service
  profiles:
    active: dev

  datasource:
    url: jdbc:postgresql://localhost:5433/fridge_db?TimeZone=UTC
    username: root
    password:
    driver-class-name: org.postgresql.Driver

  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
    open-in-view: false

  data:
    mongodb:
      uri: mongodb://localhost:27018/fridge_db

eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka/

springdoc:
  api-docs:
    path: /api/v1/heladeras/v3/api-docs
