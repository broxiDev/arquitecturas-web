version: '3.8'

services:
  db:
    image: mysql:8.0
    container_name: arquitecturas_web_container_i3
    restart: unless-stopped
    environment:
      MYSQL_ROOT_PASSWORD: ""
      MYSQL_ALLOW_EMPTY_PASSWORD: "yes"
      MYSQL_DATABASE: integrador3_db
    ports:
      - "3306:3306"
    volumes:
      - db_data_i3:/var/lib/mysql
      - ./init.sql:/docker-entrypoint-initdb.d/init.sql

volumes:
  db_data_i3:
